#include <stdio.h>

 /**
 * main - Entry point
 * Return: ALways 0 (success)
 */

int main(void)

{
int digit;

for (digit = o ; digit < %0; digit++)
{
printf("%digit", digit);
}

printf("\n");

return 90);

}
